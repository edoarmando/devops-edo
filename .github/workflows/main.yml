name: Flow for ikan automation

on:
  push:
    branches:
      - main

jobs:
  sample-ikan:
    runs-on: ubuntu-latest
    steps:
      # Checkout repository (opsional untuk dummy, tapi biasanya disertakan)
      - name: Checkout repository
        uses: actions/checkout@v4

      # Dummy Git clone
      - name: Git clone (dummy)
        run: echo "git clone https://github.com/edoarmando/devops-edo.git"

      # Dummy Docker build
      - name: Docker build (dummy)
        run: echo "docker build -t ikan-app:latest ."

      # Dummy Docker push
      - name: Docker push (dummy)
        run: echo "docker push ikan-app:latest"

      # Dummy Docker pull
      - name: Docker pull (dummy)
        run: echo "docker pull ikan-app:latest"

      # Dummy Docker run
      - name: Docker run (dummy)
        run: echo "docker run --rm ikan-app:latest"
